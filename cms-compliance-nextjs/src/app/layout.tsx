import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Knowledge Nexus Framework™ - CMS Compliance Platform",
  description: "Transforming Life Sciences Spend Management through Strategic Insourcing. Built on promoCX's Knowledge Nexus Framework© for AWS Marketplace.",
  keywords: ["CMS", "Compliance", "Life Sciences", "Pharmaceutical", "Spend Management", "HIPAA", "SOC2"],
  authors: [{ name: "promoCX" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
